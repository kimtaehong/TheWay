import dbutil


def Parse(appName, packageName, path):
    if appName == 'appA' and path == 'fileA' :
        return appA_fileA(appName, packageName, path)


def appA_fileA(appName, packageName, path):
    #blank format
    #Analysis Code
    pass